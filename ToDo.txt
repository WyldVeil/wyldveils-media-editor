App: FFmpeg (bottom right of app) says in blue text and click to update. Even after clicking it to download the latest, it still shows this. It should say FFmpeg (version) (Latest) (in green) - if latest is detected, with no clickability. Same issue for yt-dlp in the YouTube Downloader page, it just stays orange with the version number even if its the latest, as before it should be green and say (Latest) with no clickability.

User-friendliness update: In crossfader, for example: 1. When a Video Type is selected, there should be a brief summary of it somewhere appropriate and nearby. 2. When an Audio Curve is selected, there should be a brief summary of it somewhere appropriate and nearby. It's about making the app user friendly and understandable when something is just technical jargon or maybe user needs it hinted what option does what i.e. hinted at what will suit their desired outcome. This ease-of-use update should be rolled out across every tab that meets the criteria, e.g. something that currently has technical jargon that wouldn't be understood by a layman.

Youtube downloader: 1. Size info goes out of dropdown area because the text is to the right past the dropdown area size. Solution: Display size separately, to the right of the dropdown menu. 2. When cancel is clicked, it says cancelled in the console although the download continues... 3. Add a 4th option (below Direct copy, Re-encode) that is for "Specify max filesize" and then ffmpeg will download user's selection but reencode it to meet this max filesize (a bit below it) e.g. if a user needs below 500mb for discord nitro, it will save a final 498-499mb file, but if a user needs below 3mb, it'll save around 2.9mb etc. So Download mode would have options Direct copy, Re-encode, Specify max file size, and Audio only.

